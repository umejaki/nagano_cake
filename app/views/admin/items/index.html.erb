<h3>商品一覧</h3>

<table>
  <head>
    <tr>
    <th>商品ID</th>
    <th>商品名</th>
    <th>税抜価格</th>
    <th>ジャンル</th>
    </tr>
  </head>
  <% @item.each do |item| %>
  <tbody>
    <tr>
    <td><%= item.id %></td>
    <td><%= link_to item.title %></td>
    <td><%= item.introduction %></td>
    <td><%= item.price %></td>
    <td></td>
    </tr>
  <% end %>
  </tbody>
</table>